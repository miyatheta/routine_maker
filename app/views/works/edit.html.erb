<div class="container">
  <%= form_tag("/works/#{work.id}/update") do%>
    <div class="form">
      <div class="form-body">
        <textarea><%= @work.title %></textarea>
        <textarea><%= @work.detail %></textarea>
      </div>
      <div>
        <input type="submit" value="保存">
      </div>
    <% end %>
  </div>
</div>
